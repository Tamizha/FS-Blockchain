//the yes value can be handeled by converting the string value to lower case and using it, thus making all the combinations the same.


function handleInput(str){
    console.log(str.toLowerCase());
}


handleInput("yes");
handleInput("YES");
handleInput("yEs");
handleInput("Yes");
handleInput("YeS");